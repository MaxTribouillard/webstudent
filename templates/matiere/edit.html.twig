{% extends 'base.html.twig' %}

{% block title %}Modifier la matière{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-header">
            <h1 class="card-title">✏️ Modifier la matière</h1>
        </div>

        {{ include('matiere/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        <div class="btn-group" style="margin-top: 1rem;">
            <a href="{{ path('app_matiere_index') }}" class="btn btn-secondary">
                ⬅️ Retour à la liste
            </a>
            <a href="{{ path('app_matiere_show', {'id': matiere.id}) }}" class="btn btn-primary">
                👁️ Voir
            </a>
        </div>
    </div>
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}Modifier la baguette{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-header">
            <h1 class="card-title">✏️ Modifier la baguette</h1>
        </div>

        {{ include('baguette/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        <div class="btn-group" style="margin-top: 1rem;">
            <a href="{{ path('app_baguette_index') }}" class="btn btn-secondary">
                ⬅️ Retour à la liste
            </a>
            <a href="{{ path('app_baguette_show', {'id': baguette.id}) }}" class="btn btn-primary">
                👁️ Voir
            </a>
        </div>
    </div>
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}Détails de la compétence - {{ competence.libelle }}{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-header">
            <h1 class="card-title">⭐ {{ competence.libelle }}</h1>
        </div>

        <div class="detail-grid">
            <div class="detail-item">
                <div class="detail-label">ID</div>
                <div class="detail-value">{{ competence.id }}</div>
            </div>
            <div class="detail-item">
                <div class="detail-label">Code</div>
                <div class="detail-value">{{ competence.code }}</div>
            </div>
            <div class="detail-item">
                <div class="detail-label">Libellé</div>
                <div class="detail-value">{{ competence.libelle }}</div>
            </div>
            <div class="detail-item">
                <div class="detail-label">👨‍🎓 Nombre d'élèves</div>
                <div class="detail-value">{{ competence.eleves|length }}</div>
            </div>
            <div class="detail-item">
                <div class="detail-label">👩‍🏫 Nombre de professeurs</div>
                <div class="detail-value">{{ competence.professeurs|length }}</div>
            </div>
        </div>

        {% if competence.eleves|length > 0 %}
            <div class="related-section">
                <h2 class="related-title">👨‍🎓 Élèves possédant cette compétence ({{ competence.eleves|length }})</h2>
                <div class="table-responsive">
                    <table class="table">
                        <thead>
                            <tr>
                                <th>Nom</th>
                                <th>Prénom</th>
                                <th>🏯 Maison</th>
                                <th>🎯 Promotion</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for eleve in competence.eleves %}
                                <tr>
                                    <td><strong>{{ eleve.nom }}</strong></td>
                                    <td>{{ eleve.prenom }}</td>
                                    <td>
                                        {% if eleve.maison %}
                                            <a href="{{ path('app_maison_show', {'id': eleve.maison.id}) }}" class="entity-link">
                                                {{ eleve.maison.nom }}
                                            </a>
                                        {% else %}
                                            <span style="color: #9ca3af;">Aucune</span>
                                        {% endif %}
                                    </td>
                                    <td>
                                        {% if eleve.promotion %}
                                            <a href="{{ path('app_promotion_show', {'id': eleve.promotion.id}) }}" class="entity-link">
                                                {{ eleve.promotion.libelle }}
                                            </a>
                                        {% else %}
                                            <span style="color: #9ca3af;">Aucune</span>
                                        {% endif %}
                                    </td>
                                    <td>
                                        <a href="{{ path('app_eleve_show', {'id': eleve.id}) }}" class="btn btn-sm btn-primary">
                                            👁️ Voir
                                        </a>
                                    </td>
                                </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        {% endif %}

        {% if competence.professeurs|length > 0 %}
            <div class="related-section">
                <h2 class="related-title">👩‍🏫 Professeurs enseignant cette compétence ({{ competence.professeurs|length }})</h2>
                <div class="related-items">
                    {% for professeur in competence.professeurs %}
                        <a href="{{ path('app_professeur_show', {'id': professeur.id}) }}" class="badge badge-primary">
                            {{ professeur.prenom }} {{ professeur.nom }}
                        </a>
                    {% endfor %}
                </div>
            </div>
        {% endif %}

        <div class="btn-group" style="margin-top: 2rem;">
            <a href="{{ path('app_competence_index') }}" class="btn btn-secondary">
                ⬅️ Retour à la liste
            </a>
            <a href="{{ path('app_competence_edit', {'id': competence.id}) }}" class="btn btn-warning">
                ✏️ Modifier
            </a>
            {{ include('competence/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}

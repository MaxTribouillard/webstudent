{% extends 'base.html.twig' %}

{% block title %}Nouvelle Note{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-header">
            <h1 class="card-title">📝 Nouvelle Note</h1>
        </div>

        {{ include('note/_form.html.twig') }}

        <div style="margin-top: 1rem;">
            <a href="{{ path('app_note_index') }}" class="btn btn-secondary">
                ⬅️ Retour à la liste
            </a>
        </div>
    </div>
{% endblock %}

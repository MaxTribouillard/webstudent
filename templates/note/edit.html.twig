{% extends 'base.html.twig' %}

{% block title %}Modifier la note{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-header">
            <h1 class="card-title">✏️ Modifier la note</h1>
        </div>

        {{ include('note/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        <div class="btn-group" style="margin-top: 1rem;">
            <a href="{{ path('app_note_index') }}" class="btn btn-secondary">
                ⬅️ Retour à la liste
            </a>
            <a href="{{ path('app_note_show', {'id': note.id}) }}" class="btn btn-primary">
                👁️ Voir
            </a>
        </div>
    </div>
{% endblock %}

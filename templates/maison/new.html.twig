{% extends 'base.html.twig' %}

{% block title %}Nouvelle Maison{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-header">
            <h1 class="card-title">🏰 Créer une Nouvelle Maison</h1>
        </div>

        {{ include('maison/_form.html.twig') }}

        <div style="margin-top: 1rem;">
            <a href="{{ path('app_maison_index') }}" class="btn btn-secondary">
                ⬅️ Retour à la liste
            </a>
        </div>
    </div>
{% endblock %}

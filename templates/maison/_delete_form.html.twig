<form method="post" action="{{ path('app_maison_delete', {'id': maison.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette maison ?');" style="display: inline;">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ maison.id) }}">
    <button class="btn btn-danger">🗑️ Supprimer</button>
</form>

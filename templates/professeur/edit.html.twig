{% extends 'base.html.twig' %}

{% block title %}Modifier le professeur{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-header">
            <h1 class="card-title">✏️ Modifier le professeur</h1>
        </div>

        {{ include('professeur/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        <div class="btn-group" style="margin-top: 1rem;">
            <a href="{{ path('app_professeur_index') }}" class="btn btn-secondary">
                ⬅️ Retour à la liste
            </a>
            <a href="{{ path('app_professeur_show', {'id': professeur.id}) }}" class="btn btn-primary">
                👁️ Voir
            </a>
        </div>
    </div>
{% endblock %}

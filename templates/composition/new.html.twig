{% extends 'base.html.twig' %}

{% block title %}Nouvelle Composition{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-header">
            <h1 class="card-title">🧪 Nouvelle Composition</h1>
        </div>

        {{ include('composition/_form.html.twig') }}

        <div style="margin-top: 1rem;">
            <a href="{{ path('app_composition_index') }}" class="btn btn-secondary">
                ⬅️ Retour à la liste
            </a>
        </div>
    </div>
{% endblock %}
